import React, { VoidFunctionComponent } from 'react';
import { View } from 'react-native';

type OwnProps = {};

const ButtonExamples: VoidFunctionComponent<OwnProps> = () => {
  return <View></View>;
};

export default ButtonExamples;
